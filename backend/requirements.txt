# ============================================================
# backend/requirements.txt  —  PRODUCTION RUNTIME ONLY
# ============================================================
# Principle: only what the API server needs to START and SERVE.
# Dev tools (pytest, Jupyter, Scrapy, etc.) belong in requirements-dev.txt.
# ============================================================

# ── Web Framework ─────────────────────────────────────────────
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9      # FastAPI form/file uploads
pydantic>=2.6.0,<3.0.0

# ── Rate Limiting ──────────────────────────────────────────────
slowapi>=0.1.9

# ── Environment / Config ───────────────────────────────────────
python-dotenv>=1.0.0

# ── Vector Store ──────────────────────────────────────────────
chromadb>=0.5.0,<2.0.0

# ── Embeddings & Reranking ────────────────────────────────────
sentence-transformers>=2.7.0,<4.0.0
# sentence-transformers pulls torch; pin CPU-only in Dockerfile via
# --extra-index-url https://download.pytorch.org/whl/cpu

# ── LLM Clients (load only what's configured) ─────────────────
groq>=0.9.0
openai>=1.30.0               # optional – only needed if OPENAI_API_KEY is set
anthropic>=0.25.0            # optional – only needed if ANTHROPIC_API_KEY is set

# ── PDF Ingestion ─────────────────────────────────────────────
PyMuPDF>=1.23.0              # fitz – PDF loading in embedder

# ── Hybrid Retrieval ──────────────────────────────────────────
rank-bm25>=0.2.2

# ── LangChain (minimal subset) ────────────────────────────────
langchain-core>=0.2.0,<2.0.0
langchain-community>=0.2.0,<2.0.0
langchain-text-splitters>=0.2.0,<2.0.0

# ── HTTP / Async ──────────────────────────────────────────────
httpx>=0.27.0
requests>=2.32.0

# ── Utilities ─────────────────────────────────────────────────
tqdm>=4.66.0
